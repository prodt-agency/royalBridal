import { z } from 'zod'; export const paymentOrderSchema = z.object({ body: z.object({ orderId: z.coerce.number().int().positive() }), params: z.object({}), query: z.object({}) }); export const paymentVerifySchema = z.object({ body: z.object({ razorpayOrderId: z.string().min(1), razorpayPaymentId: z.string().min(1), razorpaySignature: z.string().min(1) }), params: z.object({}), query: z.object({}) });
