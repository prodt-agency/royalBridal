import { z } from 'zod'; const id = z.coerce.number().int().positive(); export const customerListSchema = z.object({ body: z.object({}), params: z.object({}), query: z.object({ page: z.coerce.number().int().min(1).default(1), limit: z.coerce.number().int().min(1).max(100).default(20), search: z.string().max(100).optional(), sort: z.enum(['createdAt', 'name']).default('createdAt'), order: z.enum(['asc', 'desc']).default('desc') }) }); export const customerIdSchema = z.object({ body: z.object({}), params: z.object({ id }), query: z.object({}) });
