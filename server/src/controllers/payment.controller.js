import { success } from '../utils/api-response.js'; import { paymentService } from '../services/payment.service.js'; export const paymentController = { createOrder: async (req, res) => success(res, { message: 'Payment order created.', data: await paymentService.createOrder(req.validated.body.orderId) }), verify: async (req, res) => success(res, { message: 'Payment verified.', data: await paymentService.verify(req.validated.body) }), webhook: async (req, res) => success(res, { message: 'Webhook received.', data: await paymentService.webhook(req.body, req.get('x-razorpay-signature')) }) };
