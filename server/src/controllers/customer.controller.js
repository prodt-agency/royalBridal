import { success } from '../utils/api-response.js'; import { customerService } from '../services/customer.service.js'; const reply = (action) => async (req, res) => success(res, { data: await action(req) }); export const customerController = { list: reply((req) => customerService.list(req.validated.query)), get: reply((req) => customerService.get(req.validated.params.id)), orders: reply((req) => customerService.orders(req.validated.params.id, req.validated.query)) };
