import { prisma } from '../lib/prisma.js';
const include = { customer: true, orderItems: { include: { product: { select: { id: true, name: true, sku: true, slug: true } } } }, payment: true, timeline: { include: { admin: { select: { id: true, name: true, email: true } } }, orderBy: { createdAt: 'asc' } } };
export const orderRepository = { list: ({ where, skip, take, orderBy }) => prisma.$transaction([prisma.order.findMany({ where, skip, take, orderBy, include }), prisma.order.count({ where })]), findById: (id) => prisma.order.findUnique({ where: { id }, include }), update: (id, data) => prisma.order.update({ where: { id }, data, include }), transition: (id, status, note, adminId, timestampField) => prisma.$transaction(async (tx) => tx.order.update({ where: { id }, data: { orderStatus: status, ...(timestampField && { [timestampField]: new Date() }), timeline: { create: { status, note, adminId } } }, include })), createTimeline: (id, status, note, adminId) => prisma.orderTimeline.create({ data: { orderId: id, status, note, adminId } }) };
