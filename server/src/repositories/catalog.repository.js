import { prisma } from '../lib/prisma.js';
const productInclude = { category: true, images: { orderBy: { sortOrder: 'asc' } }, sizes: true };
export const catalogRepository = { listCategories: () => prisma.category.findMany({ where: { active: true }, include: { children: { where: { active: true } } }, orderBy: { name: 'asc' } }), findCategory: (id) => prisma.category.findUnique({ where: { id } }), categoryUsage: (id) => prisma.category.findUnique({ where: { id }, select: { _count: { select: { children: true, products: true } } } }), createCategory: (data) => prisma.category.create({ data }), updateCategory: (id, data) => prisma.category.update({ where: { id }, data }), deleteCategory: (id) => prisma.category.update({ where: { id }, data: { active: false } }), listProducts: ({ where, skip, take, orderBy }) => prisma.$transaction([prisma.product.findMany({ where, skip, take, orderBy, include: productInclude }), prisma.product.count({ where })]), findProduct: (slug) => prisma.product.findFirst({ where: { slug, active: true, deletedAt: null }, include: productInclude }), findProductBySlug: (slug) => prisma.product.findUnique({ where: { slug }, select: { id: true } }), findProductBySku: (sku) => prisma.product.findUnique({ where: { sku }, select: { id: true } }), findProductById: (id) => prisma.product.findUnique({ where: { id }, include: productInclude }), createProduct: (data) => prisma.$transaction((tx) => tx.product.create({ data, include: productInclude })), updateProduct: (id, data) => prisma.$transaction((tx) => tx.product.update({ where: { id }, data, include: productInclude })), softDeleteProduct: (id, adminId) => prisma.$transaction((tx) => tx.product.update({ where: { id }, data: { active: false, deletedAt: new Date(), deletedById: adminId } })), restoreProduct: (id) => prisma.$transaction((tx) => tx.product.update({ where: { id }, data: { active: true, deletedAt: null, deletedById: null }, include: productInclude })) };
