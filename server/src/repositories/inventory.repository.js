export const inventoryRepository = { movement: (tx, data) => tx.inventoryMovement.create({ data }), reserve: async (tx, orderId, item) => { const movement = await tx.inventoryMovement.findUnique({ where: { orderId_productId_size_type: { orderId, productId: item.productId, size: item.size, type: 'RESERVE' } } }); if (movement) return; const product = await tx.product.updateMany({ where: { id: item.productId, active: true, deletedAt: null, stock: { gte: item.quantity } }, data: { stock: { decrement: item.quantity }, reservedStock: { increment: item.quantity } } }); if (!product.count) throw new Error('INVENTORY_UNAVAILABLE'); const size = await tx.productSize.updateMany({ where: { productId: item.productId, size: item.size, stock: { gte: item.quantity } }, data: { stock: { decrement: item.quantity }, reservedStock: { increment: item.quantity } } }); if (!size.count) throw new Error('INVENTORY_UNAVAILABLE'); await tx.inventoryMovement.create({ data: { orderId, productId: item.productId, size: item.size, quantity: item.quantity, type: 'RESERVE' } }); }, transition: async (tx, orderId, item, type) => { const prior = await tx.inventoryMovement.findUnique({ where: { orderId_productId_size_type: { orderId, productId: item.productId, size: item.size, type } } }); if (prior) return; const productData = type === 'COMMIT' ? { reservedStock: { decrement: item.quantity } } : { stock: { increment: item.quantity }, reservedStock: { decrement: item.quantity } }; await Promise.all([tx.product.update({ where: { id: item.productId }, data: productData }), tx.productSize.update({ where: { productId_size: { productId: item.productId, size: item.size } }, data: productData })]); await tx.inventoryMovement.create({ data: { orderId, productId: item.productId, size: item.size, quantity: item.quantity, type } }); } };
