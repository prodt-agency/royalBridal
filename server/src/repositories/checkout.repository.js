import { prisma } from '../lib/prisma.js';
export const checkoutRepository = { transaction: (callback) => prisma.$transaction(callback, { isolationLevel: 'Serializable' }), productsByIds: (tx, ids) => tx.product.findMany({ where: { id: { in: ids }, active: true, deletedAt: null }, include: { sizes: true } }), findCustomer: (tx, email, phone) => tx.customer.findFirst({ where: { OR: [{ phone }, ...(email ? [{ email }] : [])] } }), upsertCustomer: async (tx, data) => { const current = await tx.customer.findFirst({ where: { OR: [{ phone: data.phone }, ...(data.email ? [{ email: data.email }] : [])] } }); return current ? tx.customer.update({ where: { id: current.id }, data: { name: data.name, email: data.email ?? current.email, phone: data.phone } }) : tx.customer.create({ data: { name: data.name, email: data.email, phone: data.phone } }); }, createOrder: (tx, data) => tx.order.create({ data, include: { customer: true, orderItems: { include: { product: true } }, payment: true, timeline: true } }) };
