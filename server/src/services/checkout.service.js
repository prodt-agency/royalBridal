import crypto from 'node:crypto'; import { ConflictError, NotFoundError } from '../utils/app-error.js'; import { checkoutRepository } from '../repositories/checkout.repository.js'; import { inventoryService } from './inventory.service.js';
const shipping = Object.freeze({ STANDARD: 0, EXPRESS: 250 }); const number = () => `RB-${new Date().getFullYear()}-${crypto.randomBytes(5).toString('hex').toUpperCase()}`;
export const checkoutService = { create: (payload) => checkoutRepository.transaction(async (tx) => { const grouped = new Map(); payload.items.forEach((item) => { const key = `${item.productId}:${item.size}`; grouped.set(key, { ...item, quantity: (grouped.get(key)?.quantity ?? 0) + item.quantity }); }); const items = [...grouped.values()]; if (items.some((item) => item.quantity > 10)) throw new ConflictError('Maximum quantity per product size is 10.'); const products = await checkoutRepository.productsByIds(tx, items.map((item) => item.productId)); if (products.length !== new Set(items.map((item) => item.productId)).size) throw new NotFoundError('One or more products are unavailable.'); const lines = items.map((item) => { const product = products.find((entry) => entry.id === item.productId); const size = product.sizes.find((entry) => entry.size === item.size); if (!size) throw new NotFoundError(`Size ${item.size} is unavailable.`); return { ...item, price: Number(product.salePrice ?? product.price) }; }); const customer = await checkoutRepository.upsertCustomer(tx, payload); const subtotal = lines.reduce((total, item) => total + item.price * item.quantity, 0); const shippingAmount = shipping[payload.shippingMethod]; const totalAmount = subtotal + shippingAmount; const order = await checkoutRepository.createOrder(tx, { orderNumber: number(), customerId: customer.id, customerName: customer.name, email: customer.email, phone: customer.phone, addressLine1: payload.addressLine1, addressLine2: payload.addressLine2, city: payload.city, state: payload.state, pincode: payload.pincode, notes: payload.notes, shippingMethod: payload.shippingMethod, shippingAmount, totalAmount, paymentMethod: payload.paymentMethod, paymentStatus: 'PENDING', orderStatus: 'PENDING', orderItems: { create: lines.map((item) => ({ productId: item.productId, selectedSize: item.size, quantity: item.quantity, price: item.price })) }, payment: { create: { method: payload.paymentMethod, status: 'PENDING', amount: totalAmount } }, timeline: { create: { status: 'PENDING', note: 'Order created.' } } }); for (const item of lines) await inventoryService.reserve(tx, order.id, item); return { order, customer, amount: totalAmount, payment: order.payment }; }) };
