import { dashboardRepository } from '../repositories/dashboard.repository.js'; export const dashboardService = { overview: async () => { const [revenue, todayOrders, statusGroups, totalCustomers, totalProducts, activeProducts, lowStockProducts, recentOrders] = await dashboardRepository.stats(); return { totalRevenue: revenue._sum.totalAmount ?? 0, todayOrders, ordersByStatus: Object.fromEntries(statusGroups.map((entry) => [entry.orderStatus, entry._count])), totalCustomers, totalProducts, activeProducts, lowStockProducts, recentOrders }; } };
