import { Router } from 'express'; import { paymentController } from '../controllers/payment.controller.js'; import { asyncHandler } from '../utils/async-handler.js'; import { validate } from '../middlewares/validate.middleware.js'; import { paymentOrderSchema, paymentVerifySchema } from '../validators/payment.validator.js'; const router = Router(); router.post('/create-order', validate(paymentOrderSchema), asyncHandler(paymentController.createOrder)); router.post('/verify', validate(paymentVerifySchema), asyncHandler(paymentController.verify)); router.post('/webhook', asyncHandler(paymentController.webhook)); export default router;
