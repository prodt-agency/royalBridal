import { Router } from 'express'; import { catalogController } from '../controllers/catalog.controller.js'; import { asyncHandler } from '../utils/async-handler.js'; import { requireAdmin, requireRole } from '../middlewares/auth.middleware.js'; import { validate } from '../middlewares/validate.middleware.js'; import { categoryCreateSchema, categoryIdSchema, categoryUpdateSchema, productCreateSchema, productQuerySchema, productSlugSchema, productUpdateSchema } from '../validators/catalog.validator.js';
const categories = Router(); const products = Router();
categories.get('/', asyncHandler(catalogController.categories)); categories.post('/', requireAdmin, requireRole('SUPER_ADMIN', 'ADMIN'), validate(categoryCreateSchema), asyncHandler(catalogController.createCategory)); categories.patch('/:id', requireAdmin, requireRole('SUPER_ADMIN', 'ADMIN'), validate(categoryUpdateSchema), asyncHandler(catalogController.updateCategory)); categories.delete('/:id', requireAdmin, requireRole('SUPER_ADMIN'), validate(categoryIdSchema), asyncHandler(catalogController.deleteCategory));
products.get('/', validate(productQuerySchema), asyncHandler(catalogController.products)); products.get('/:slug', validate(productSlugSchema), asyncHandler(catalogController.product)); products.post('/', requireAdmin, requireRole('SUPER_ADMIN', 'ADMIN', 'MANAGER'), validate(productCreateSchema), asyncHandler(catalogController.createProduct)); products.patch('/:id', requireAdmin, requireRole('SUPER_ADMIN', 'ADMIN', 'MANAGER'), validate(productUpdateSchema), asyncHandler(catalogController.updateProduct)); products.delete('/:id', requireAdmin, requireRole('SUPER_ADMIN', 'ADMIN'), validate(categoryIdSchema), asyncHandler(catalogController.deleteProduct)); products.post('/:id/restore', requireAdmin, requireRole('SUPER_ADMIN', 'ADMIN'), validate(categoryIdSchema), asyncHandler(catalogController.restoreProduct));
export { categories, products };
