import multer from 'multer'; import { ValidationError } from '../utils/app-error.js'; const upload = multer({ storage: multer.memoryStorage(), limits: { fileSize: 5 * 1024 * 1024, files: 10 }, fileFilter: (req, file, callback) => callback(null, ['image/jpeg', 'image/png', 'image/webp'].includes(file.mimetype)) }); export const productUpload = (req, res, next) => upload.array('images', 10)(req, res, (error) => next(error ? new ValidationError(error.message) : null));
